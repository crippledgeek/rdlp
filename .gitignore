# Rust build artifacts
/target
**/target
# NOTE: Cargo.lock is NOT ignored - it should be committed for binary projects

# Debug artifacts
**/*.rs.bk
*.pdb
# Stray binary from ad-hoc `cargo build`/run output redirection at repo root
/rust_out

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Test artifacts
*.profraw
*.profdata

# Temporary files
*.tmp
*.temp

# Machine-specific Cargo config (contains local paths)
.cargo/config.toml

# Plugin build artifacts
plugins/*/target/

# Plugin build artifacts produced by `rdlp plugin build-from-ytdlp`
# (and signed via `scripts/sign-plugin.sh`). Each ported plugin lives at
# `examples/plugins/<name>/<name>.py` (source — TRACKED, byte-identical
# to upstream yt-dlp); the build output goes to a sibling directory
# `examples/plugins/<name>/<name>/` containing `plugin.wasm` (~35 MB),
# `plugin.toml.template` (unsigned manifest), and `plugin.toml` (signed
# with the build host's Ed25519 key). All three regenerate on every
# build and MUST NOT be committed — both because they're large binaries
# and because the signed manifest carries a per-developer key. The
# goldens build the wasm at test time from the .py source, so this
# directory is purely an install-staging artefact.
examples/plugins/*/*/plugin.wasm
examples/plugins/*/*/plugin.toml
examples/plugins/*/*/plugin.toml.template
docs/
CLAUDE.md
debug_tnaflix.rs
.claude/

# Downloaded media files (never commit)
*.mkv
*.mp4
*.avi
*.mov
*.flv
*.ts
*.webm
*.ogg
*.m4a
*.mp3
*.flac
*.wav

# Downloaded subtitle files
*.vtt
*.srt
*.ass
*.ssa
*.lrc

# Windows null device redirect artifact
nul

# Node.js / Frontend
node_modules/
package-lock.json
crates/rdlp-desktop/dist/

# Un-ignore TypeScript files in desktop frontend (*.ts above targets MPEG-TS video)
!crates/rdlp-desktop/**/*.ts
!crates/rdlp-desktop/**/*.tsx

# superpowers brainstorm + plan scratch
.superpowers/

# graphify knowledge graph output (per-machine artifact)
graphify-out/
.graphifyignore
